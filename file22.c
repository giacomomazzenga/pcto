//il programma calcola il codice fiscale 
#include <stdio.h>
#include <string.h>
#include <unistd.h>

  char cognome1 (char a [20],  char b [20])
{
    a == ( 'b' || 'c' || 'd' || 'f' || 'g' || 'h' || 'j');
    gets(a<4);
    printf ("le lettere del cognome del codice fiscale sono:%s", a  );
   return ( a );
}

int main ()
{
    char cognome [20];
    char nome [20];
    char sesso [20];
    char luogo [20];
    char provincia [20];
    char data [20];
    char lettere [50];
    lettere == ('b' || 'c' || 'd' || 'f' || 'g' || 'h' || 'j');
    printf("inserisci il tuo cognome");
    scanf("%s", cognome);
    printf("inserisci il tuo nome");
    scanf("%s", nome);
    printf("inserisci il tuo sesso (M/F)");
    scanf("%s", sesso);
    //printf("inserisci il tuo luogo di nascita");
    //scanf("%s", luogo);
    //printf("inserisci la tua provincia (sigla)");
    scanf("%s", provincia );
    printf("inserisci la tua data di nascita");
    scanf("%s", data );
    printf("calcolo in corso...");
    if ( cognomeContains(lettere) );
    {
        cognome1(lettere);
    }
   
    // H501
}


