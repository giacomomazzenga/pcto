//il programma calcola il codice fiscale 
#include <stdio.h>
#include <string.h>
#include <unistd.h>

float divisione(float a,  float b)
{

}
int main ()

    char cognome [20];
    char nome [20];
    char sesso [20];
    char luogo [20];
    char provincia [20];
    char data [20];
    printf("inserisci il tuo cognome");
    scanf("%s", cognome);
    printf("inserisci il tuo nome");
    scanf("%s", nome);
    printf("inserisci il tuo sesso (M/F)");
    scanf("%s", sesso);
    printf("inserisci il tuo luogo di nascita");
    scanf("%s", luogo)
    printf("inserisci la tua provincia (sigla)")
    scanf("%s", provincia );
    printf("inserisci la tua data di nascita");
    scanf("%s", data );
    printf("calcolo in corso...");
    sleep (1000);
    if (cognome = B && C && D && F && G && H && J && K && L && M && N && P && Q && R && S && T && U && V && W && X && Y && Z) 
    {
        float cognome(char cognome)
    }
    // H501



